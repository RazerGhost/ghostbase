node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example

# Local dev SQLite DBs (production paths are set via env vars, on a Coolify
# volume outside the repo)
/data

vite.config.js.timestamp-*
vite.config.ts.timestamp-*
